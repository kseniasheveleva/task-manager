{{#if isShowing}}
<div
  class="fixed {{ type }} animate-fade-up animate-duration-500 border-t-4 flex items-center w-full max-w-xs p-4 space-x-4 text-gray-500 bg-white divide-x rounded-lg shadow bottom-5 right-5 "
  role="alert">
  <div class="text-sm font-normal">{{ message }}</div>
</div>
{{/if}}